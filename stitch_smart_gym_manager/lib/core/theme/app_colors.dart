import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ===== Primary =====
  static const Color primary = Color(0xFFC3F400);
  static const Color primaryDark = Color(0xFFABD600);

  // ===== Background =====
  static const Color background = Color(0xFF051424);
  static const Color surface = Color(0xFF122131);
  static const Color surfaceLight = Color(0xFF1C2B3C);

  // ===== Text =====
  static const Color textPrimary = Color(0xFFD4E4FA);
  static const Color textSecondary = Color(0xFFC4C9AC);

  // ===== Status =====
  static const Color success = Color(0xFFC3F400);
  static const Color warning = Color(0xFFFFB800);
  static const Color error = Color(0xFFFF4D4D);
  static const Color info = Color(0xFF00F0FF);

  // ===== Border =====
  static const Color border = Color(0xFF333333);
}